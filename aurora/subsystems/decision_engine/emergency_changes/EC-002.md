# Emergency Change EC-002

## Status

ACTIVE

## Trigger

Initiative #56 reached its explicit Initiative review boundary with no ACTIVE Phase or Step.

During required closeout, a BLOCKING Engineering Finding was identified because Planning Hansel does not route a clean-context worker to the existing `INITIATIVE_CLOSEOUT.md` authority.

The canonical Engineering Discovery `submit_finding()` operation then failed while attempting to resolve optional Planning provenance. Planning correctly reported that the ACTIVE Initiative has no ACTIVE Phase, but Engineering Discovery converted that legitimate review-boundary state into a submission error instead of capturing the Finding without invented Step provenance.

## Authorization

Explicit human authorization for Emergency Change EC-002 was granted on 2026-09-08.

Emergency authority is limited to the minimum work necessary to diagnose, repair, validate, and document Engineering Finding submission at a legitimate Planning review boundary where no ACTIVE Step exists.

## Governing Principle

> When process prevents correct work, the process has become its own defect.

The required normal path is:

```text
Initiative review
    ↓
qualified BLOCKING Engineering Finding
    ↓
Engineering Discovery persistence
    ↓
Planning remedial work
    ↓
closeout resumes
```

EC-002 exists only to restore that path.

## Steps Taken

1. Initiative #56 reached `INITIATIVE_REVIEW_REQUIRED`.
2. Closeout discovered that Planning Hansel does not expose the existing `INITIATIVE_CLOSEOUT.md` authority.
3. Engineering Discovery qualification and blocking authorities established the condition as a BLOCKING `DEAD_END`.
4. Attempted canonical `submit_finding()`.
5. Submission failed because optional provenance resolution treated the legitimate no-ACTIVE-Step review state as an unsafe Planning read.
6. Human explicitly authorized EC-002.

## Current Recovery Objective

Restore `submit_finding()` so a qualified Finding encountered at a legitimate Planning review boundary can be persisted without fabricated Step provenance.

Do not alter Planning lifecycle state merely to make Finding submission work.

Do not perform unrelated cleanup.

Return to normal Engineering Discovery and Planning authority immediately after the defect is repaired and validated.

## Validation

PENDING

## Return to Normal Authority

PENDING
