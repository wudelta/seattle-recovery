# Project Aurora: Strategic Plan & Workspace State Token
**Target Objectives Matrix for Session Date: 2026-05-24**
*Status: Architecture Verified, Headless Core Active, Assets Synced to Git*

---

## 1. Active System State Profile (Where We Left Off)
* **The Brain Core**: Django is successfully converted into a Headless JSON Data Engine. 
* **Morning Initialization (`start_online_session.py`)**: Fully verified via browser-free terminal unit tests (`OK`). Ingests staging brief texts, logs PostgreSQL metrics, and compiles namespaced 5-day historical summary context instructions loops cleanly.
* **Evening Cleanup (`end_session_view.py`)**: Fully verified via terminal testing (`OK`). Generates text summaries, purges raw chatter, closes session metrics, and spawns an isolated, asynchronous background thread that pushes active repository snapshots straight to **GitHub** in `2.655 seconds` without blocking or freezing the web worker process.
* **Interface Layer (`landing.html`)**: Pristine single-screen unscrollable Morph layout view completely recovered. **Vis Network node canvas visualization graphics** are fully operational, styled with dark slate text palettes, and loop-proof zoom adjustments.

---

## 2. Active Backlog Objectives (Tomorrow's Checklist)

### Phase 1: Verification & First Handshake (Morning Workflow)
- [ ] **Task 1.1**: Connect to the laptop terminal, spin up `python manage.py runserver`, and verify that `http://127.0.0` boots straight to the clean bluish-gray neumorphic layout.
- [ ] **Task 1.2**: Run the independent backend testing validation command block to confirm environmental sanity checks right away: `python manage.py test aurora.tests`

### Phase 2: Google Drive Automated Backup Integration (Evening Framework Upgrade)
- [ ] **Task 2.1**: Audit your remote authentication keys and credentials scripts for Google Drive cloud storage access paths.
- [ ] **Task 2.2**: Integrate your automated database backup script directly into **Stage 3** of the asynchronous off-thread background worker inside `end_session_view.py`.
- [ ] **Task 2.3**: Verify that when a session closes down, the database backup file streams directly to your cloud storage link before the thread shuts down.

### Phase 3: Headless Console Frontend & Project Plan Automation
- [ ] **Task 3.1**: Connect the local **Llama 8B minion worker** to your newly registered `update_project_plan_api` endpoint path.
- [ ] **Task 3.2**: Run terminal checks to verify that the 8B minion accurately modifies this file, ticking off completed checkboxes (`- [x]`) and appending new milestones headlessly without adding conversation chatter.
- [ ] **Task 3.3**: Tie the 8B project plan update function directly into the evening close down views sequence to make the automation loop absolute.

---

## 3. Core Guardrails (Never Disobey)
1. **Never write frontend HTML template code blocks inside your Django backend files.** All data views must return pure, structured JSON payloads.
2. **Never troubleshoot file path parameters or API connection routes inside a web browser.** Always test data streams directly from your command line terminal using independent unit testing suites or `curl` localhost queries.
3. **Always offload network-bound operating system commands (such as cloud pushes or database backups) to a detached asynchronous background thread** to keep your development server lightning-fast.
