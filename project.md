

## Backlog Export Session Cluster (2026-06-10 20:32:40)
* [ ] update the api_commands, the forge_registry, the page_skeleton, and the api_skeleton modules to output success or failure messages to the STDOUT and add a terminal panel to the blueprint_console_panel to display them.
* [ ] add a unlocked_components panel to the blueprint_console_panel that displays unlocked records from the postgres aurora_componentregistry and have a lock button that allows me to lock them.
* [ ] in aurora/api/api_commands.py there is a delta_notes_endpoint function. in aurora/api/delta_notes_api.py there is also a delta_notes_endpoint function. which one is the console using.
* [ ] in the DELTA_NOTES_CONSOLE_PANEL in the CAPTURE_INTENTION panel make the text input box  and buttons fill the available space.
