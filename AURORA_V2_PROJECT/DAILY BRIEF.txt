i have created an api called hopehub/api/get_content_api.py. right now it returns placeholder data that will be fine to test with. we will add the real logic after we get the /bind statement working.

the /bind command that is executed from aurora/api/api_commands.py. there are already /page and /api routines that create UI layer HTML and endpoints respectively. this api will take arguments: app_name function_name api_name. when invoked it will the function_name to make the call to api_name and modify the function_name.html file that currently displays an under construction page to display the api JASON payload.

the /bind command will be issued from the aurora_console to the aurora/api/api_commands.py which routs it to aurora/api/handlers/api.py. i will feed you those to files next.
