i want to add the daily brief back into the project. i also need to add the project.md in there as well. this should take care of the whole project plan feature i wanted to add. much more simple. i can just feed the project.md and daily brief into genesis or wu at the start of each session. the end session routine will be update the project.md and commit to git. that simple. much better than that complicated thing i was building.

i also need to add the delta_directives back in. i now know how that should be structured. directives will be stored in the database by target minion or wu. it will contain the rules for each minion or wu. then the minions need to be created.

wu: uses the llama 70B model. will have his own directives. no code writing. orchestration only. figure out what i want and pass it off to a minion. helps me write the directives for each minion as dense .md files by minion name. reads his directives, the project.md and the daily_brief for the day at the start of each session, updates the project.md and suggests steps for completion. updates the project.md file at the end of each session.

html_minion: comment anchors to make it clear where a div is located, what it contains and what it calls. no inline css or js. this will be moved into the static folders with the same name as the html file. only the extension will change. html will leverage bootstrap and the bootswatch themes, only writing custom css when directed.

js_minion: comment anchors by function parts. js will use jquery and not do database operations. database calls will be done through an api only. output start and end of functions to stdout. if it fails silently i should be able to see it didn't complete without analyzing the browser output.

python_minion: comment anchors for each subprocess. front end and back end processes will be decoupled. there will be an api for database calls and any complex tasks. the web server will only route api calls to the back end receiving json objects back.

sql_minion: queries the postgress database. receives directives from wu.

graph_minion: queries the neo4j database. receives directives from wu.

doc_minion: updates module documentation stored in the postgress database. will be triggered by minion completing a task.

test_minion: writes the tests for each module. will be triggered by minion completing a task.


Project Overview: i am Delta, the project architect. the project name is Seattle Recovery. It contains two applications, aurora and hopehub that each have a web UI. aurora is an AI (Wu) assisted application builder that automates development tasks. it's purpose is to build hopehub. hopehub is a recovery platform to assist people experiencing homelessness, substance use disorder, and mental health issues. it's purpose is to provide information on resources and services available to these people. it will act as an automated case manager to develop an action plan for both physical, and emotional well-being. hopehub will also be AI assisted for the end users.

Development Environment: the project is developed in python using django as the web framework. it uses djangorestframework to decouple the UI from the logic. the UI uses bootstrap with bootswatch themes. the javascript uses jquery. the databases are postgres and neo4j. postgress handles the django admin and other django processes such as login. it also is the repsitory for user information that must be encrypted and secured. neo4j will be used by the AI to find relationships to resources based on user needs for hopehub. for aurora it will track application model dependencies and targets. 

Current State: aurora a landing page, login page and a console for development. it also has a robust application skeleton builder that creates either a user interface or a back end api that does the heavy lifting of database queries and delivers a json payload back to the UI. the skeleton builder is accessed through the console and does not rely on AI assistance so it works completely off-line.

Development Constraints: development is being done on an ubuntu laptop with 2 cores and 8GB ram. the project currently has one developer, Delta. this project has a very low budget and can only utilize free, open source software. the project must conform class DeltaDirectives(models.Model):
    """
    Standalone configuration engine storing system instructions, prompts,
    and model processing boundaries for your AI minion fleet.
    """
    id = models.UUIDField(primary_key=True, default=uuid.uuid4, editable=False)
    directive_name = models.CharField(
        max_length=255, 
        unique=True, 
        db_index=True,
        help_text="The system lookup name identifying this minion (e.g., 'minion_AI_writer')."
    )
    instructions = models.TextField(help_text="The absolute master system prompt directive for this model.")
    constraints = models.JSONField(
        default=dict, 
        blank=True, 
        help_text="Stores engine controls like {'model': 'llama-3.1-8b-instant', 'temperature': 0.3}"
    )
    is_active = models.BooleanField(default=True, help_text="Toggle to enable or disable this minion profile.")
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        verbose_name = "Delta Directive Profile"
        verbose_name_plural = "Delta Directive Profiles"

    @classmethod
    def provision_standard_minions(cls) -> int:
        """Programmatically seeds the database with default settings for the core minion fleet."""
        minion_fleet = {
            "minion_wu": {
                "instructions": "Act as the master project orchestrator. Parse complex multi-step tasks, evaluate repo requirements, and delegate isolated tasks down to the specialized 8B fleet.",
                "constraints": {"model": "llama-3.3-70b-versatile", "temperature": 0.1}
            },
            "minion_UI_layout": {
                "instructions": "Generate structural layouts. Produce clean, well-formed HTML skeleton layout blocks based on context.",
                "constraints": {"model": "llama-3.1-8b-instant", "temperature": 0.4}
            },
            "minion_UI_style": {
                "instructions": "Generate interface style themes. Output clean utility or custom CSS styling rules.",
                "constraints": {"model": "llama-3.1-8b-instant", "temperature": 0.3}
            },
            "minion_UI_logic": {
                "instructions": "Generate client interactivity. Output pure modern JavaScript block strings code blocks.",
                "constraints": {"model": "llama-3.1-8b-instant", "temperature": 0.2}
            },
            "minion_anamod": {
                "instructions": "Analyze existing repository code modules. Propose clean code modifications or file patches safely.",
                "constraints": {"model": "llama-3.1-8b-instant", "temperature": 0.1}
            },
            "minion_AI_writer": {
                "instructions": "Refactor raw text blocks. Polish clarity, style, documentation records, and structural layout phrasing.",
                "constraints": {"model": "llama-3.1-8b-instant", "temperature": 0.6}
            }
        }

        seeded_count = 0
        for name, data in minion_fleet.items():
            obj, created = cls.objects.get_or_create(
                directive_name=name,
                defaults={
                    "instructions": data["instructions"],
                    "constraints": data["constraints"],
                    "is_active": True
                }
            )
            if created:
                seeded_count += 1
        return seeded_count

    def __str__(self):
        return f"{self.directive_name} [Active: {self.is_active}]"

