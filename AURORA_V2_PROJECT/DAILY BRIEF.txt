i am adding a code editor to my application builder written in python/django. it is web based and i want it to be my primary IDE. i have an ubuntu laptop for development. everything runs in docker containers. the application is called aurora and the aurora_console is where everything happens. it has a view selector that dynamically loads snippets for different functions. the code editor is called anamod. it is partially functional. it mimics the spyder which is what i currently use, but it uses monaco for the editor. it has a tree file selector on the left, a view port in the middle and command buttons and a terminal on the right that displays what is happening. the tree works and the terminal works. when i select a file, i see in the terminal that it reads it and the title of the view port changes to indicate the file selected. the view port, however does  not  display the contents of the file or allow me to type anything. i don't want you to write any code until you have read what is there. this should all happen off-line. i don't want to be dependent on a wifi connection to view and edit code.
