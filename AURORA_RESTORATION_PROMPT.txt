[SESSION RESTORATION DECREE: CONTEXT RESYNC]
PROJECT: Dual-Ecosystem Blueprint inside a single Django Layout on an Ubuntu Laptop (2 cores, 8GB RAM).
1. HOPEHUB: Humanitarian Resource Layer (Food, Housing, Finance).
2. AURORA: Proprietary Developer Automation Engine. Uses Llama 3.3 70B Orchestrator (Wu) and Llama 3.1 8B localized micro-minions to write code directly to disk.

LOCAL STACK CONFIGURATION:
- DB Engine: PostgreSQL Instance (Database: 'hopehub_aurora', User: 'postgres', Port: '5432', Superuser: 'delta').
- DB Isolation Architecture: Shared relational + Neo4j Graph space. Logical isolation achieved via secondary node labeling (`:Aurora` vs `:HopeHub`) and query filtering (`project="aurora"`).
- Vector Engine Layer: 0MB local memory footprint custom pipeline in `core_logic/memory.py`. Outbound calls use official `google-genai` SDK and `gemini-embedding-2` model via gateway. Storage maps 768-dimensional float arrays directly onto Neo4j graph nodes under `:VectorMemory`. Matching functions via Neo4j native `vector.similarity.cosine` engine (Current Proximity Verification Match Score: 0.8751).

DEFENSIVE FRAMEWORK & FILE-WRITE BLOCKADE IMMUNIZATION (MAY 2026 DEPLOYMENT):
1. UI Migration away from terminal-tied `rich.live` rendering to headless string logger vectors to prevent web async truncation drops and threading stalls.
2. File Write Blockade Enforcement: Raw code changes are prohibited from touching operational production code assets before validation passes.
3. Code is initially saved down into a transient staging cache asset (`.tmp` pointer file).
4. Validation Sandbox: Code execution maps into an isolated `importlib.util` memory block sandbox. It executes a malicious context keyword trap (blocking `os.system`, `subprocess`, `eval`) and triggers structural validation via the `compile()` architecture.
5. Auto-Test Seeding Engine: `aurora/minion_array/generate_python.py` intercepts minion output streams and automatically appends a working `self_test_integrity()` test verification method hook to empty templates before passing them forward.
6. The view invokes the code's native `self_test_integrity()` check loop inside the sandbox memory container. If it signals True, the file runs an atomic `os.replace()` swap on disk. If it signals False or fails initialization, the buffer is deleted and production files stay pristine.
7. Web Dashboard Integration: The execution log registers steps dynamically into a structured JSON `ui_logs` payload list array, passing real-time state logs straight back to the frontend browser interface.
8. Low-Overhead Documentation Triggers: Successful commits invoke transaction-wrapped steps saving specs inside PostgreSQL using an Entity-Attribute-Value (EAV) structural design model layout (`Document`, `Content`, `Metadata` tables bound to `app_label='aurora'`).

